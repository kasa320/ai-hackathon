# Codex 向けリポジトリ指示

このリポジトリは、AI HACK 2026 向けの輪読運営エージェントを実装する作業場所です。現行の要件と未確定事項は `.agent/decisions/spec.md`、開発順は `.agent/decisions/plan.md` を参照してください。

## 現在の進め方

- 調査、アイデア、意思決定、ピッチ準備の記録は `.agent/kasa/` に置く。構成と記録ルールは `.agent/kasa/README.md` を参照する。
- 採用題材は輪読運営。既存会の辞退・準備状況の変化から、再計画・本人の引き受け・同意・保存・通知までをMVPとする。LifeOps等は過去の候補として記録を残す。
- 実際の利用事例には固定の幹事がおらず、メンバー同士がLINEで調整していた。試作の接点はWeb＋Discord。評価は `.agent/decisions/evaluation.md` に従い、参加者全体の調整負担を測る。
- 当面、仕様・設計・評価資料は `.agent/` 内に置く。`docs/` の追加・更新はユーザーが再開を指示するまで行わない。
- 用途固有の型・判断指示・検証・承認条件は `internal/playbook/<用途>/` に分離する。共通側は `coord.Playbook` に依存し、用途別パッケージの登録は起動処理で行う。構成は `.agent/decisions/architecture.md` を参照する。
- フロント・バックエンド間の公開APIは `.agent/decisions/api.md` の契約に従う。内部型で公開契約を上書きせず、変更時は仕様・モック・生成OpenAPIを揃える。
- 実装に着手する前に、対象ユーザー、MVPの単一デモフロー、外部連携の範囲、承認が必要な操作、成功指標を明文化する。
- 方針を決めたら、理由と却下した選択肢を `.agent/kasa/decisions/` に残す。既存の調査・アイデアの懸念点や却下理由は削除しない。
- 外部情報には確認日と参照 URL を記録し、事実・推測・提案を区別する。

## 実装時の方針

- 短期間で、入力の解釈、計画、ツール実行、結果確認、失敗時の再計画、人による承認を含む一つの完結した業務フローを優先する。
- 実サービスの認証や仕様に過度に依存せず、必要ならデモ用の連携先を用意する。
- 個人情報、費用、契約に関わる操作は、保存方法と人間の承認地点を設計に含める。
- 新しい依存関係、実行方法、環境変数を導入したら README に追記する。

## コミット

- 変更は機能または目的ごとに小さく分ける。
- コミットメッセージは簡潔な1行とし、`feat:`、`fix:`、`docs:`、`style:`、`refactor:`、`perf:`、`test:`、`chore:` のいずれかを先頭に付ける。
